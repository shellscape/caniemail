checkCommands:
  fix: pnpm format && pnpm lint
  lint: pnpm lint
  types: pnpm typecheck
  test: pnpm test

# Proactive behaviors currently run once per day at 00:00 PST (beta; controlled by Charlie).
# This playbook is intended to be the daily caniemail.com data refresh (time-of-day not enforced by this repo).
beta:
  proactive:
    - playbook: '.charlie/playbooks/update-caniemail-json.md'
